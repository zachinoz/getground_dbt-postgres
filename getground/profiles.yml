getground:
  outputs:

    dev:
      type: postgres
      threads: 4
      host: localhost
      port: 5432
      user: <username>
      pass: <password>
      dbname: getground_test
      schema: source

    prod:
      type: postgres
      threads: 4
      host: localhost
      port: 5432
      user: <username>
      pass: <password>
      dbname: getground_test
      schema: source

  target: dev

getground:
  outputs:

    dev:
      type: postgres
      threads: [1 or more]
      host: [host]
      port: [port]
      user: [dev_username]
      pass: [dev_password]
      dbname: [dbname]
      schema: [dev_schema]

    prod:
      type: postgres
      threads: [1 or more]
      host: [host]
      port: [port]
      user: [prod_username]
      pass: [prod_password]
      dbname: [dbname]
      schema: [prod_schema]

  target: dev
